# دليل دمج المجالات من لوحة التحكم
## Vendor Fields Integration Guide

---

## ✅ النظام الحالي | Current System

### 1. جدول المجالات في قاعدة البيانات

**الجدول:** `vendor_fields`

**الأعمدة:**
- `id` - المعرف الفريد
- `name` - اسم المجال بالعربية
- `name_en` - اسم المجال بالإنجليزية
- `is_active` - حالة المجال (نشط/غير نشط)
- `created_at` - تاريخ الإنشاء
- `updated_at` - تاريخ التحديث

### 2. المجالات المتاحة حالياً (15 مجال)

```
1.  ألمنيوم وزجاج (Aluminum & Glass)
2.  أرضيات وبلاط (Flooring & Tiles)
3.  بناء وتشييد (Construction)
4.  تصميم داخلي (Interior Design)
5.  تكييف وتبريد (HVAC)
6.  جبس وديكور (Gypsum & Decoration)
7.  حدادة (Blacksmithing)
8.  دهان (Painting)
9.  سباكة (Plumbing)
10. صيانة عامة (General Maintenance)
11. عزل (Insulation)
12. كهرباء (Electricity)
13. مقاولات عامة (General Contracting)
14. نجارة (Carpentry)
15. نظافة (Cleaning)
```

---

## 🔄 كيف يعمل الجلب في النموذج | How Fetching Works

### الكود في `Step1BasicIdentity.tsx`:

```typescript
const fetchFields = async () => {
  try {
    setIsLoadingFields(true);
    setFieldsError('');

    // جلب المجالات من قاعدة البيانات
    const { data, error } = await supabase
      .from('vendor_fields')          // من جدول vendor_fields
      .select('name')                 // اختيار عمود name فقط
      .eq('is_active', true)          // المجالات النشطة فقط
      .order('name');                 // ترتيب أبجدي

    if (error) {
      setFieldsError('حدث خطأ في تحميل المجالات');
      return;
    }

    if (data && data.length > 0) {
      setFields(data.map(f => f.name)); // حفظ المجالات في state
    } else {
      setFieldsError('لا توجد مجالات متاحة');
    }
  } catch (error) {
    setFieldsError('حدث خطأ في تحميل المجالات');
  } finally {
    setIsLoadingFields(false);
  }
};
```

### التدفق:
1. ⏳ يبدأ التحميل ← يظهر أيقونة `Loader2` الدوارة
2. 🔍 يرسل استعلام لقاعدة البيانات
3. ✅ إذا نجح ← يحفظ المجالات في state
4. ❌ إذا فشل ← يعرض رسالة خطأ
5. 🎯 ينتهي التحميل ← يعرض القائمة المنسدلة

---

## 📋 إدارة المجالات من لوحة التحكم

### طريقة الوصول:
`Dashboard → Settings → إعدادات المجالات`

### الملف المسؤول:
`src/components/admin/settings/SupplierFieldsSettings.tsx`

### العمليات المتاحة:

#### 1. **إضافة مجال جديد**
```sql
INSERT INTO vendor_fields (name, name_en, is_active)
VALUES ('مجال جديد', 'New Field', true);
```

#### 2. **تعطيل مجال**
```sql
UPDATE vendor_fields
SET is_active = false
WHERE id = 'field_id';
```

#### 3. **تفعيل مجال**
```sql
UPDATE vendor_fields
SET is_active = true
WHERE id = 'field_id';
```

#### 4. **حذف مجال**
```sql
DELETE FROM vendor_fields
WHERE id = 'field_id';
```

---

## 🎯 المميزات الحالية

### في النموذج:
- ✅ **تحميل تلقائي**: يتم جلب المجالات عند فتح الصفحة
- ✅ **مؤشر تحميل**: أيقونة دوارة أثناء الجلب
- ✅ **معالجة الأخطاء**: رسائل واضحة عند حدوث خطأ
- ✅ **بحث ذكي**: يمكن البحث في المجالات
- ✅ **ترتيب أبجدي**: المجالات مرتبة حسب الحروف
- ✅ **فلترة تلقائية**: يعرض فقط المجالات النشطة

### في لوحة التحكم:
- ✅ **إضافة مجالات**: إضافة مجالات جديدة
- ✅ **تعديل مجالات**: تعديل الأسماء
- ✅ **تفعيل/تعطيل**: التحكم في الظهور
- ✅ **حذف مجالات**: حذف المجالات غير المستخدمة

---

## 🔧 كيفية إضافة مجال جديد

### من لوحة التحكم:

1. **انتقل إلى الإعدادات**
   ```
   Dashboard → Settings → إعدادات المجالات
   ```

2. **اضغط على "إضافة مجال"**

3. **املأ البيانات:**
   - الاسم بالعربية: مثل "تنسيق حدائق"
   - الاسم بالإنجليزية: مثل "Landscaping"
   - الحالة: نشط

4. **احفظ**
   - سيظهر المجال الجديد فوراً في نموذج التسجيل

### مباشرة من قاعدة البيانات:

```sql
INSERT INTO vendor_fields (name, name_en, is_active)
VALUES ('تنسيق حدائق', 'Landscaping', true);
```

---

## 🔍 اختبار النظام

### 1. تأكد من وجود البيانات:
```sql
SELECT name, is_active
FROM vendor_fields
ORDER BY name;
```

### 2. افتح نموذج التسجيل:
```
/vendor-registration
```

### 3. تحقق من القائمة المنسدلة:
- انقر على حقل "المجال الأساسي"
- يجب أن ترى 15 مجال
- يجب أن تكون مرتبة أبجدياً

### 4. اختبر البحث:
- اكتب "كهر" ← يجب أن يظهر "كهرباء"
- اكتب "سب" ← يجب أن يظهر "سباكة"

---

## ⚠️ ملاحظات مهمة

### 1. **المجالات المعطلة**
- المجالات التي `is_active = false` لا تظهر في النموذج
- يمكن تفعيلها مرة أخرى من لوحة التحكم

### 2. **الحذف**
- عند حذف مجال، تأكد من عدم استخدامه في موردين حاليين
- يفضل التعطيل بدلاً من الحذف

### 3. **الترجمة**
- يتم عرض `name` (العربي) في النموذج
- `name_en` للاستخدام المستقبلي

### 4. **الأداء**
- يتم جلب المجالات مرة واحدة عند تحميل الصفحة
- لا يؤثر على سرعة النموذج

---

## 🔗 الملفات ذات الصلة

### Frontend:
```
src/components/vendor-registration/steps/Step1BasicIdentity.tsx
└── يجلب المجالات ويعرضها في القائمة المنسدلة

src/components/admin/settings/SupplierFieldsSettings.tsx
└── واجهة إدارة المجالات في لوحة التحكم
```

### Database:
```
supabase/migrations/20260109170536_add_vendor_fields_and_cities_tables.sql
└── ملف الهجرة الذي أنشأ جدول vendor_fields
```

---

## 📊 تدفق البيانات

```
┌─────────────────────────────────────────────────────────────┐
│                    قاعدة البيانات                           │
│                   vendor_fields table                        │
│  (15 مجال متاح - الكل نشط is_active = true)                │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ Supabase Query
                      │ .from('vendor_fields')
                      │ .select('name')
                      │ .eq('is_active', true)
                      │
                      ↓
┌─────────────────────────────────────────────────────────────┐
│              نموذج تسجيل الموردين                           │
│           Step1BasicIdentity Component                       │
│                                                              │
│  ┌──────────────────────────────────────────┐              │
│  │  ⏳ Loading State                        │              │
│  │  - يعرض أيقونة Loader2 الدوارة         │              │
│  └──────────────────────────────────────────┘              │
│                                                              │
│  ┌──────────────────────────────────────────┐              │
│  │  ✅ Success State                        │              │
│  │  - يعرض 15 مجال في القائمة المنسدلة    │              │
│  │  - مرتبة أبجدياً                        │              │
│  │  - قابلة للبحث                          │              │
│  └──────────────────────────────────────────┘              │
│                                                              │
│  ┌──────────────────────────────────────────┐              │
│  │  ❌ Error State                          │              │
│  │  - رسالة خطأ باللون الأحمر              │              │
│  └──────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────┘
                      │
                      │ User Selection
                      │
                      ↓
┌─────────────────────────────────────────────────────────────┐
│           VendorFormData.primary_field                       │
│         (يتم حفظ المجال المختار هنا)                        │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ خلاصة

النظام يعمل بالكامل! ✨

- 🔄 **الجلب التلقائي**: يتم جلب المجالات من `vendor_fields` تلقائياً
- 🎯 **التحديث الفوري**: أي تغيير في لوحة التحكم يظهر فوراً
- 🛡️ **معالجة الأخطاء**: رسائل واضحة عند حدوث مشاكل
- 🎨 **واجهة سلسة**: loading states وbest practices

لا حاجة لأي تعديلات! النموذج جاهز ويعمل بشكل مثالي. 🚀

---

**آخر تحديث**: 2026-02-17
**الحالة**: ✅ يعمل بكفاءة
**عدد المجالات**: 15 مجال متاح
